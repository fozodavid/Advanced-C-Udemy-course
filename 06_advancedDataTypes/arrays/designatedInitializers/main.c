// #include <stdio.h>

// struct point { int x, y; };
  /*
struct Point {
  int x, y, z;
};

int main() {


  int a[6] = {[4] = 29, [2] = 15};
  int b[6] = {[4]25, [2]15};

  int c[5] = { 0, 0, 15, 0, 29, 0};

  int numbers[100] = {1,2,3,[3 ... 9] = 10, [10] = 80, 15, [70]=50, [42] = 42};

  for (int i = 0; i < 20; i++) {
    printf("%d ", numbers[i]);
  }

  printf("\n%d ", numbers[70]);
  printf("%d", numbers[42]);
  return 0;

  // struct point p = { .y = 2, .x = 3};
  // struct point p = { y: 2, x: 3};
  // struct point p = { 2, 3};

  struct Point p1 = {.y = 3, .z = 1, .x = 2};
  struct Point p2 = {.x = 20, .y = 2};

  printf("x = %d, y = %d, z = %d\n", p1.x, p1.y, p1.z);
  printf("x = %d, y = %d\n", p2.x, p2.y);
  return 0;
}
  */
